with Text_io,CHAINES,BusStop_package;use Text_io,CHAINES,BusStop_package;


procedure Main is 
    Seconde : constant duration := 1.0;
    B1 : BusStop_package.BusStop;
    B2 : BusStop_package.BusStop;    
begin
    put_line("gogogogogo");
    delay(5*Seconde);
    --S1.receiveDisplay("yopyopyoypypo");
    --put_line(B1.name);
end Main;
